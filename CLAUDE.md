# Clubmast — marketing site

**Astro** site (`astro.config.mjs`), with `core-backend/` and `core-dashboard/` alongside
and a `deploy.sh`. `90_day_sprint_plan.md` carries the plan.

- `dist/` is build output — never hand-edit it.
- This is the marketing surface for the Clubmast app (`clubmast-app` repo); keep claims here
  consistent with what the app actually does.
## House rules

- **Never suggest GitHub Actions or any CI.** It is disabled account-wide on purpose — the
  global CLAUDE.md explains why. Run suites locally and say so instead.
